version: '3'

services:
  next-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Aquila-front
    ports:
      - '3000:3000'
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
    command: npm run dev
